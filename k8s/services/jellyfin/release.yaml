apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jellyfin
  namespace: jellyfin
spec:
  interval: 1h
  chart:
    spec:
      chart: jellyfin
      version: 2.7.0
      sourceRef:
        kind: HelmRepository
        name: jellyfin
        namespace: jellyfin
