extraContainers:
  - name: m3uparser
    image: danifuu/m3uparser:latest
    envFrom:
      - secretRef:
          name: jellyfin-secret
    volumeMounts:
    - mountPath: /usr/src/app/VODS
      name: media
    - mountPath: /usr/src/app/logs/
      name: media

persistence:
  config:
    size: 20Gi # Increased size to store logs/metadata
