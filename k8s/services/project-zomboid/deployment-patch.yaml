apiVersion: apps/v1
kind: Deployment
metadata:
  name: zomboid-dedicated-server
spec:
  template:
    spec:
      nodeSelector:
        node-role.kubernetes.io/worker: ""
      containers:
      - name: zomboid-dedicated-server
        envFrom:
        - secretRef:
            name: project-zomboid-config
